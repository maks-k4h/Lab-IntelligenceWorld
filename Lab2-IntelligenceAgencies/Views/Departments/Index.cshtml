@model IEnumerable<Department>

@{
    ViewData["Title"] = "Департаменти";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>@ViewData["Title"]</h1>

<ul class="nav nav-pills">
    <li class="nav-item">
        <a class="nav-link" asp-action="Create">Створити новий</a>
    </li>
</ul>

<table class="table">
    <thread>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Id)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Agency)
            </th>
            <th></th>
        </tr>
    </thread>
    <tbody>
    
    @foreach (var access in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(item => access.Id)
            </td>
            <td>
                @Html.DisplayFor(item => access.Name)
            </td>
            <td>
                @Html.DisplayFor(item => access.Agency.Name)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@access.Id">Редагувати</a> |
                <a asp-action="Delete" asp-route-id="@access.Id">Видалити</a>
            </td>
        </tr>
    }
    
    </tbody>
    
</table>
